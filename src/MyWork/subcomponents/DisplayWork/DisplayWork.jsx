import React from 'react';
import './displaywork.styles.scss';
import '../../mywork.images.scss';

function DisplayWork(props) {
  return (
    <div className={`${props.className} display-work`} />
  );
}

export default DisplayWork;
